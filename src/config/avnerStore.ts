// 🗄️ ARCHIVE: "אבנר" store template
// -----------------------------------------------------------------------------
// The Avner Ovad (אבנר עובד) storefront — sign override, codedScene and the
// internal store page — is preserved in code but not attached to any store.
//
// To apply the Avner store to a specific store, set AVNER_STORE_ID to that
// store's id (e.g. "s3", "s7"). Set it back to null to archive it again.
//
// A single change here flips both the mall card and the internal store page.
export const AVNER_STORE_ID: string | null = null;